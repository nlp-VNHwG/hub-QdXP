• 什么是前后端分离？
1. 前后端分离：前端负责页面的渲染，后端负责数据的处理和返回。
• 历史对话如何存储，以及如何将历史对话作为大模型的下一次输入；
1. 每次请求都使用AdvancedSQLiteSession类，共用一个session。openai-agents SDK会存储历史对话，自动拼接完整历史再发给大模型。
2. 每轮对话结束，都会将消息写入数据库。
3. 当后端接收到一个不存在的sessionID时，会创建一个新的session。