# 作业2

### 1.什么是前后端分离

- 前端主要负责数据的渲染，展示
- 后端处理数据，业务逻辑
- 通常为不同的工程，前端资源文件单独存放在一个工程文件，后端每个服务若使用微服务设计模式，则每个服务单独为一个工程

### 2.历史对话如何存储，以及如何将历史对话作为大模型的下一次输入

- 问答完成后通过生成的session_id调用append_message2db方法 将对话缓存存放在关系型数据库之中
- 根据session_id通过调用get_chat_sessions接口查询出用户历史对话记录，在初始化对话时传给大模型