作业一：
bert 文本分类和 实体识别有什么关系，分别使用什么loss？

关系：
文本分类和实体识别都属于多任务学习，都共享底层的一个bert模型来进行预训练。
文本分类：输出的是pooler_output，它代表整个句子的语义，通过最后一个全链接层会输出一个整个句子分类的概率。
实体识别：输出的是token_output,它代表的是一个字的上下文语义，最终会输出每个字具体的标签概率。

文本分类和意图识别的loss_function都是self.criterion = nn.CrossEntropyLoss()。
文本分类中使用这个主要是用来预测句子级别分类的误差。
实体识别：计算的是每个字词的分类误差。

多任务训练  loss = seq_loss + token_loss 有什么坏处，如果存在训练不平衡的情况，如何处理？
坏处：文本分类和句子识别本身就不属于一个量级，计算出的loss的量级，以及收敛的速度都不一样，可能会导致有一方的任务无法很好地完成，存在冲突。

处理：
设置一个权重，seq_loss * x + token_loss * （1-x）,通过不断训练寻找最优x.
梯度剪裁与分类：底层共享的bert使用较小的学习率，但是上层的分类器使用较大的学习率，避免某一个任务的梯度过度破坏预训练特征。


  作业二：
  #role
你是一个专业的NLU系统引擎，你的任务是理解用户的查询意图，并且转化为计算机可精切处理和执行的结构化数据。

#task
请阅读用户的输入文本，从中准确地提取出以下三个维度的信息：
1.domin（领域类别）
2.intent（意图类别）
3.slots（实体槽位）

# Constraint
必须且只能从以下列表中选择 domain 和 intent。 
【待选领域 domain】: music, app, radio, lottery, stock, novel, weather, match, map, website, news, message, contacts, translation, tvchannel, cinemas, cookbook, joke, riddle, telephone, video, train, poetry, flight, epg, health, email, bus, story 
【待选意图 intent】: OPEN, SEARCH, REPLAY_ALL, NUMBER_QUERY, DIAL, CLOSEPRICE_QUERY, SEND, LAUNCH, PLAY, REPLY, RISERATE_QUERY, DOWNLOAD, QUERY, LOOK_BACK, CREATE, FORWARD, DATE_QUERY, SENDCONTACTS, DEFAULT, TRANSLATION, VIEW, NaN, ROUTE, POSITION 
必须根据文本内容抽取具体的 slots，如果不存在某个实体，请不要捏造。常见的实体包括但不限于：Src(出发地), Dest(目的地), name(名称), time(时间), artist(艺术家), ingredient(食材) 等。
最终结果必须以纯 JSON 格式输出，不要包含任何额外的解释文本或 Markdown 标记。

#输出格式JSON：
{
  "domain": "匹配的领域 ",
    "intent": "匹配的意图",
    "slots": {
      "实体类型1": "text中的原始词汇"
      "实体类型2": "text中的原始词汇"
    }
}

#输出模版示例
{
    "text": "清蒸鱼怎么做？吴？",
    "domain": "cookbook",
    "intent": "QUERY",
    "slots": {
      "dishName": "清蒸鱼"
    }
  },
