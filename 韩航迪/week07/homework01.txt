问题一：
两者均用 BERT 提取特征。文本分类取 [CLS] 向量做句子级预测，用交叉熵损失；实体识别对每个 token 做序列标注，用 Token 级交叉熵损失（常结合 CRF）。核心区别在于分类粒度：前者整句，后者逐词。
问题二：
直接相加易致大 Loss 主导梯度，引发收敛冲突。解决：1. 静态加权：手动平衡量级；2. 动态加权：用不确定性加权或 GradNorm 自动学习权重；3. 分阶段训练。推荐动态方法自适应调节。
