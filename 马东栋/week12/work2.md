# 什么是前后端分离？
- 前后端分离就是前后端不写在一起，前端负责和用户交互，通过与用户交互获取用户的需求，然后通过需求进行转发请求到后端服务器，后端负责处理请求，后端在收到请求之后利用前端传来的数据进行业务处理，一般是一些CRUD操作，操作完成之后将处理结果返回给前端，前端进行数据的展示。
# 历史对话如何存储，以及如何将历史对话作为大模型的下一次输入；
- 此项目中，历史对话通过session进行存储，用户的历史消息存在session对象中，然后进行持久化的存储，存进sqlite中，在用户下次提问时，将历史对话作为大模型的下一次输入。